<%= form_for(@mobile_message) do |f| %>
  <% if @mobile_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mobile_message.errors.count, "error") %> prohibited this mobile_message from being saved:</h2>

      <ul>
      <% @mobile_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
